def find(arr,n,x):
    # code here
    ans=[]
    for i in range(n):
        if(arr[i]==x):
            ans.append(i)
    if(len(ans)==1):
        return ans*2
    elif(len(ans)>1):
        return ans[0],ans[-1]
    else:
        return [-1,-1]
