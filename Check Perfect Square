class Solution{
    static int checkPerfectSquare(int N){
        // code here
        int i = 1;
        while (i * i < N) {
            i++;
        }

        if (i*i == N) {
            return 1;
        }
        return 0;
    }
}
