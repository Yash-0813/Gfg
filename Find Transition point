def transitionPoint(arr, n):
    #Code here
    if(arr[0]==1):
        return 0
        
    for i in range(len(arr)-1):
        if(arr[i]==0 and arr[i+1]==1):
            return i+1
        
    return -1
