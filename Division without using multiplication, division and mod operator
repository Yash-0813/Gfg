class Solution:
    def divide(self, a, b):
        #code here
        flag= 1
        if a < 0:
           flag*= -1
           a = -a
        if b < 0:
           flag*= -1
           b = -b
        return (a//b)*flag
